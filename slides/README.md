# Student slide copies (export only)

**Do not edit HTML or PDF in this folder.** They are copies.

Source of truth: `../../slides/sessionNN-*/` (see `_project/SLIDES.md`).

On **publish** after class: copy the knitted classroom HTML and the print PDF here with the dated `70-467_MLBA_YYYY-MM-DD_Title` names, then update `index.qmd`. Canvas gets **this** PDF.
